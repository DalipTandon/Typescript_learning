//A tuple is a typed array with a pre-defined length and types for each index.

let user:[number,string]=[13,"hell"]; 

//tuples helps in defining that at what array index what datatype can be filled , according to aboe example we ca't have string at 0th index
//tuples helps in predefining the datatype of indexes